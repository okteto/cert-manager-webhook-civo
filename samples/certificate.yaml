apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: default-ssl-certificate
  namespace: default
spec:
  dnsNames:
  - '*.example.com'
  issuerRef:
    kind: Issuer
    name: civoissuer
  secretName: default-ssl-certificate