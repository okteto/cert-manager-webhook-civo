apiVersion: v1
kind: Service
metadata:
  name: {{ include "certmanager-civo.fullname" . }}
  labels:
    {{- include "certmanager-civo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "certmanager-civo.selectorLabels" . | nindent 4 }}
